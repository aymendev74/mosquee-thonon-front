on:
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Branche de release (par ex. 1.x)'
        required: true

permissions:
  contents: write

jobs:
  create-release-branch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Create release branch
        run: |
          git config --global user.name "Aymen"
          git config --global user.email "aymen330d@hotmail.fr"
          RELEASE_BRANCHE="release-${{ github.event.inputs.release_version }}"
          git checkout -b $RELEASE_BRANCHE
          npm version ${{ github.event.inputs.version }} --no-git-tag-version
          git add package.json
          git commit -m "release branche created ==> ${{ github.event.inputs.release_version }}"
          git push origin $RELEASE_BRANCHE
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
